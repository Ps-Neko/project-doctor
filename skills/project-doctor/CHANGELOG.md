# 변경 이력 (CHANGELOG)

## v1.6.0 — 2026-06-12 (HARD-02 검진 일관성 — BL-01·02)
- HARD-02(반복 매직 넘버) 탐지 기준을 명확화: **대상**(요율·한도·임계 등 비즈니스/설정 값)과 **제외**(0·1·-1, 배열 인덱스, 화면 좌표·크기·여백 등 구조적 수치) 구분 + **DUP-01 흡수 규칙**(중복 코드 블록이 원인이면 DUP-01로 흡수, 이중 보고 금지) 공개판 이식
- 효과: 이전엔 검진자마다 매직 넘버 꼬리를 다르게 잡던 것이, 신선 검진자 3명 재측정에서 **전원 동일 판단**(messy의 50000·0.05 등을 DUP-01로 흡수)·탐지율 100% 일관
- 진단 카탈로그 정의 변경(사용자 가시 동작) — 픽스처·정답지는 무변경(HARD-02는 채점 중립 유지)

## v1.5.3 — 2026-06-12 (비긴급 백로그 — 위생 정리)
- DEMO.md: 발견ID 블록을 보고서 맨 마지막 줄로 이동(BL-26) — 데모 보고서가 자기 도구의 형식 검증(verify FORM-05)을 통과하도록. "## 이 다음은?" 절을 발견ID 앞으로
- check_links: 이미지 임베드(`![]()`)·코드펜스 안 예시 링크를 검사에서 제외(오탐 방지) + 단위 테스트 6건 신설
- 출력 이모지(`✓`) 제거 — 사용자 환경 규칙(이모티콘 금지) 정합 (check_links·run_checks)
- docstring BACKLOG 번호에 역할 명시(BL-15: CI 링크 검사 / CI 채점 회귀)
- 코드 동작·채점·보고서 계약 무변경 (문서·출력·검사 견고성)

## v1.5.2 — 2026-06-12 (전체 코드 리뷰 결함 수정 — 채점기·검증기 파싱 견고성)
- **verify check_grade**: 코드펜스 안 예시 등급줄이 펜스 밖 실제 위반 등급을 가리던 Critical 우회 수정(펜스 제외 + 등급줄 전수 검사). 종합 판정 헤딩 부제 허용(startswith)
- **verify SECRET_PATTERNS**: GitHub 가변길이 토큰(`ghp_{36,255}`)·`gho_`/`ghs_`/`github_pat_` 변종·Google API 키(`AIza…`)·줄바꿈 든 PEM 추가 — 비밀키 거짓음성 축소
- **verify check_four_fields**: 전역 substring 카운트 → 발견 항목(### 헤딩)별 검사로 거짓양성·거짓음성 제거. 발견ID:(없음)인데 본문 발견 있으면 모순 검출(기계블록 정상일 때만)
- **verify read_text**: `errors="replace"` 추가(cp949 보고서 트레이스백 → compare와 일관화). detect_major_version은 표지 '스킬 버전:' 줄로 한정(본문 과거버전 오판 방지)
- **발견ID 블록**: verify는 발견ID 줄이 여러 개(본문/부록 예시 + 실제)면 FORM-05로 잡고, compare는 펜스 안 예시 발견ID를 채점에서 제외 — 둘이 다른 줄을 보던 불일치 해소
- **compare parse_expected_ids**: 전각 파이프(｜)·백틱 셀·'발견 ID' 공백 헤더 허용, 같은 절 2표에서 id_col 리셋, 'GUID' 함정 헤더 정확 매칭, 절 진입 정확화('## 심은 문제 해설' 후속 절 누출 차단)
- **compare parse_neutral_ids**: ISO-8601 등 비-ID 토큰이 앞서도 카탈로그 접두사 ID 우선 채택
- 회귀 테스트 +17(펜스 우회·비밀키 신형·열 인덱싱·2표·전각/백틱/공백헤더·cp949 등). 진단·보고·채점 계약 무변경

## v1.5.1 — 2026-06-12 (코드 리뷰 결함 수정 — 검증자 실측 재현 8건)
- **[Critical] check_write_boundary**: 드라이브 상대경로(`C:foo`)·UNC가 "프로젝트 안"으로 오판되어 경계를 우회하던 결함 수정(Windows 실측: 파일이 루트 밖에 생성됨) — 드라이브 있고 루트 없는 경로는 거부, UNC·절대경로는 루트 결합 없이 해석. TOCTOU(심링크/정션 교체) 한계 문서화 + 통과 시 해석경로 출력
- check_write_boundary: 빈/공백 경로도 거부(인자 실수 침묵 통과 방지)
- compare_report: cp949 보고서에서 트레이스백으로 죽던 것 수정(`read_text`에 `errors="replace"` — 형제 스크립트와 일관화)
- check_scoring_regression: 합성 보고서를 소스트리 고정 파일명 → OS 임시폴더(tempfile)로(병렬 레이스·잔존물·동명파일 파괴 제거), 채점기와 동일한 ID 추출(`parse_expected_ids`) 재사용, "발견 N개 중 M개 채점" 분모 출력(전부 통과 착시 제거)
- check_version: 마커는 있는데 버전 표기만 깨진 경우를 '표기 없음'과 구분해 실패로 집계(드리프트 사각지대) + 단어경계 정규식(`rev10.20.30` 오매칭 방지)
- check_links: 마크다운 title 문법·쿼리스트링 분리로 멀쩡한 링크 오탐 제거
- 단위 테스트 +3(드라이브 상대·UNC·빈 경로, Windows 한정 skipif), 양축 CI(ubuntu+windows) 통과. 진단·보고·채점 계약 무변경

## v1.5.0 — 2026-06-12 ("쓰기 경계 자동 검사")
- 치료 실행 안전 강화: `tools/check_write_boundary.py` 신설 — 변경 예정 경로가 프로젝트 폴더 밖(절대 경로·`../` 탈출)을 가리키면 비0 종료. 스킬과 함께 설치돼 치료 직전 호출 가능. 읽기 전용(아무것도 수정 안 함)
- prescription-protocol 0장 4번(쓰기 경계 확인)에 도구 호출 안내 추가 — 도구 없으면 기존 수동 규칙으로 폴백
- 단위 테스트 7건(경계 안/밖/절대경로/혼합/루트오류) 추가, CI에 포함. 진단·보고·채점 계약 무변경

## v1.4.1 — 2026-06-12 (측정 보강 — 외부 리뷰 반영)
- compare_report.py: 정답지·채점중립 어디에도 없는 ID 보고 시 자동 미달 게이트(기존엔 참고 출력만). leaky 정답지 채점중립 절 정합
- CI(GitHub Actions) 추가: pytest·자체 픽스처 채점 회귀·문서 링크 검사. tests/run_checks.py(검증 묶음 러너)·check_links.py 신설
- README: 참조 문서 수량(8개) 정정, 품질근거에 "내부 표본 기준"+한계 고지. 스킬 동작·보고서 계약 무변경

## v1.4.0 — 2026-06-12 ("클리니컬 결과지 + PDF 직접 출력")
- HTML 결과지를 **"클리니컬" 디자인**으로 확정 (시안 3종 비교 선택): 종합병원 검진센터 결과지 스타일 — 타이포그래피 위계·가는 괘선 중심, 이모지·아이콘 장식 금지, 색은 판정·심각도에만 최소 사용. CSS 골격·클래스를 디자인 계약으로 고정
- **PDF 직접 출력 신설**: 이 컴퓨터에 이미 있는 브라우저(Edge/Chrome headless)를 감지해 HTML→PDF 자동 변환 — 별도 설치 0, 감지 실패 시 "브라우저 인쇄→PDF 저장" 안내로 폴백. 형식 질문에 ③ PDF 추가
- 형식 변형 묶음 확장: 같은 보고서의 .md+.html+.pdf = 보고서 1건 (절대 규칙 7)
- 보고서 내용·채점 계약 무변경

## v1.3.0 — 2026-06-12 ("처방 미리보기 + 보고서 형식 선택")
- 처방 실행 게이트 확장: 변경 예정 "파일 목록"을 "파일 목록 + 전/후 변경 내용 미리보기"로 — 승인 전에 무엇이 어떻게 바뀌는지 일상 언어 설명과 함께 확인, 실행 중 미리보기 밖 변경이 필요해지면 중단 후 재제시
- 보고서 저장 시 형식 선택 신설 (`references/report-formats.md`): ① 마크다운(기본) ② **HTML 인쇄용** — "병원 결과지" 디자인(등급 원형 배지·건수 칩·판정 알약·처방 강조 상자·심각도별 색 테두리 카드), 더블클릭→브라우저→인쇄(Ctrl+P)→"PDF로 저장"으로 PDF화, 워드에서도 열림 ③ 워드(.docx)는 pandoc이 설치된 컴퓨터에서만 변환(설치 유도 금지)
- HTML 자기완결 의무: 인라인 CSS·외부 리소스(CDN/웹폰트) 금지·`<script>` 금지·UTF-8 — 오프라인/사내망에서도 안전하게 열림. 기계 판독 줄(발견ID:)은 문서 끝에 작게 포함(생략 금지), 채점 정본은 여전히 md/채팅
- 보고서 형식 검증기 신설 (`tests/verify_report_format.py` + pytest 11건): 표지·스킬 버전 줄·등급↔표현 고정표 정합·4요소 개수·기계 판독 블록 위치·비밀키 값 노출 휴리스틱을 기계 검사
- SKILL.md: 보고서 공통 의무에 "내보내기 전 자가 점검" 추가, 절대 규칙 7 보정(같은 보고서의 형식 변형 .md+.html = 보고서 1건)
- 보고서 내용·채점 계약(발견ID 블록) 무변경 — 형식은 겉포장만

## v1.2.0 — 2026-06-11 ("결과지 개편")
- 보고서 머리를 '건강검진 결과지' 3블록으로 재설계: 표지(환자·검진일) → 종합 판정(등급 크게 + 읽는 법 범례 + 의사 소견) → 부위별 소견(신호등 판정) + 오늘의 처방(당장 1건·재검 권고)
- 등급↔표현 고정표 신설 (A🟢건강 / B🟢양호 / C🟡주의 / D🔴치료 필요) — 회차 간 판정어 흔들림 차단
- 소견-판정 톤 정합 규칙 ("B인데 '하나도 없습니다'"식 충돌 금지), 종합 등급 산정 설명("평균이 아니라 합산")을 보고서 머리로 이동
- 상세 소견(ID+4요소)과 기계 판독 계약(발견ID 블록)은 변경 없음 — 채점기 영향 0

## v1.1.0 — 2026-06-10 (v1.1 컷)
- 공개 전 검진(release-check): 키 형식별 패턴(SEC-01) + git 과거 기록 점검(SEC-02) + 개인정보(PII-01) + 라이선스·기록부 잔존(REL) + 통과/보류 판정(시점 기재 + 한계 고지 3종) + 폴더 검사 제외 선언
- 초진 차트(intro): 프로젝트 지도(핵심 파일 5개)·실행법·위험 구역·미완성 구간 + 인수인계 소견서 옵션(양방향)
- 방향전환(pivot): 갈림길 비교(2차 소견) + 유지/수정/폐기 매트릭스 + 마일스톤("멈춰도 동작") + 폐기 2단계
- 접수 문진 완전판(4모드 라우팅), 픽스처 3종(leaky·handover·pivot) + 정답지, 전체 회귀 + 개밥먹기
- 라이선스 MIT 확정, 스킬 전용 배포 저장소 분리 (Q3·Q6 해소)

## v1.0.0 — 2026-06-10 (v1.0 컷)
- 정밀 검진(--deep) 추가: git 이력 핫스팟(HIST-01), 의존성 대조(DEP-01/02), 검사 장비 통역(LINT-01, 코드 실행 승인 게이트), AI 작업물 문진(AIGEN, 보수적 판정)
- 진단 카탈로그 25개+ 본문 완성 (기본 검진 22항목 + 정밀 전용), 규모 가드(파일 100개 초과 시 축소 제안)
- 진료기록부: `.project-doctor/records/` 생성 승인 게이트 + 자체 .gitignore + 재검진 비교(해결/그대로/신규/악화)
- README 완성판(필수 고지 3종) · LICENSE · CHANGELOG · 채점 스크립트 pytest
- 정식 측정: 신선한 세션 3회 검진, 최저치 기준 80% 이상 통과

## v0.3.0 — 2026-06-10
- 실행 처방전 프로토콜: 실행 게이트(파일 목록 제시·사전 질문) → 체크포인트 → 변경 → 검증 사다리 → 전/후 비교 → 되돌리기 한 줄 항상 인쇄
- git 없는 환경 대체 절차(백업/거부 시 실행 차단), 쓰기 경계(프로젝트 루트 한정)

## v0.2.0 — 2026-06-10
- 건강검진(진단·보고) 활성화: ID 불변 계약 + 진단 카탈로그 코어 10항목
- 테스트 픽스처(messy-project) + 정답지(EXPECTED.md) + 기계 채점 스크립트 — 드라이런 탐지율 100%
- 보고서 공통 골격 최종 동결 (검진 결과표 + ID+4요소 + 기계 판독 블록)

## v0.1.0 — 2026-06-10
- 뼈대: 접수 문진, 인자 해석 표, 절대 규칙 12개, 모드 라우팅, README 스텁
