# 변경 이력 (CHANGELOG)

## v2.7.12 — 2026-06-16 (8차 외부 평가 정합 — 골든·측정 최신화 + HTML 주석 XSS 차단)
- **[Security] HTML 검사기 허위 종료 주석(`<!-->`) 우회 차단** (verify_html_report.py): `_HtmlAuditor`에 주석 미검사 사각이 있어, `<!-->`(브라우저는 빈 주석을 즉시 닫지만 파이썬 `html.parser`는 뒤따르는 마크업을 주석으로 삼킴)로 `<img onerror=…>` 같은 XSS 페이로드가 통과하던 갭(8차 평가 P1, 실측 재현)을 봉합. `handle_comment`로 주석 DATA의 허용목록 밖 태그(HTML-01)·이벤트 핸들러(HTML-02)를 스캔하고, 보조로 빈 급종료 주석 토큰(`<!---?>`)을 raw 검사. 골격의 정상 주석(`<!-- 재검진이면… -->`)은 false-fail 0. 회귀 테스트 +3. 검사기 한정 — 진단 카탈로그·채점 무변경.
- **[Fix] 골든 검진 보고서 버전 동결 차단** (check_version.py): 골든 `tests/golden/checkup-report.{md,html}` 표지가 v2.7.7에 4버전 동결돼 있었는데 parity·형식 검증기·E2E 어느 게이트도 'SKILL 대비 stale'를 잡지 못하던 구조적 사각(8차 평가 P0)을 차단 — 버전 정합 검사 대상에 골든 2파일(`스킬 버전:` 마커)을 추가해 'SKILL.md == 골든 표지'를 CI가 강제. 골든 표지·parity 하드코딩을 v2.7.12로 동반 갱신.
- **[Test] 보안 픽스처 leaky 골든 E2E 추가** (tests/golden/leaky-report.md): 유일한 보안 픽스처(SEC/PII)인 leaky가 골든 보고서가 없어 형식·위치 게이트가 E2E로 한 번도 안 돌던 공백(8차 평가 P2)을 메움. 시크릿 값은 마스킹(키 값 미출력 계약). CI·pytest에 3게이트 통과 회귀 고정 — 위치 게이트가 SEC-01(app.py·config.js)·PII-01(notes.md)까지 검사.
- **[Docs] EVALS 측정 기록 v2.7.11까지 최신화**: 검진표가 v2.7.8에 멈춰 코드(v2.7.11)보다 stale하던 것을, 새 측정이 아닌 'CI 게이트 통과 기록' 행 + 탐지 영향 분석 v2.7.11 연장으로 보강(v2.7.9~11은 검사기·보안·CI·문서 한정이라 탐지율 불변).
- **[Docs] README·report-formats 정합 보강** (8차 평가): ① README 품질 근거 '3회 100%'에 측정 시점(v1.0.0·release-check v2.0.1) 단서 ② checkup 행 '35종 ID'→'기본 22항목' 정정 ③ report-formats §5에 `verify_report_format.py`는 미배포(CI/측정용) 도구임을 명시 ④ messy 정답지 `추가 보고 허용`에 --deep 정답(HIST-01·DEP-01) 등재로 정직한 --deep 보고서가 오탐 미달되지 않게 정합.
- 검사기·CI·문서·테스트·정답지/골든만 변경. 진단 카탈로그(ID)·채점 알고리즘·픽스처 본체 무변경 → 탐지율 불변. (채택 9·기각 3·보류 17 상세: 개발 저장소 BACKLOG O.)

## v2.7.11 — 2026-06-15 (보안 심층방어 — 비밀파일 .gitignore · 신뢰 불가 경로 따옴표 명문화)
- **[보안] `.gitignore`에 비밀파일 패턴 추가** (`.env`·`*.pem`·`*.key`): 이 저장소엔 진짜 비밀이 없지만 향후 실수 커밋을 예방하는 심층 방어. 테스트 픽스처 `.env.backup`(의도된 가짜 비밀)은 영향 없음(`.env.*`는 쓰지 않음).
- **[보안][Docs] 신뢰 불가 경로 따옴표·내용 비보간 주의 명문화** (prescription-protocol §2 검증 공통 · release-checklist §1 셸 명령 공통): 남의 프로젝트를 상대로 셸 명령을 돌릴 때 파일 경로는 따옴표로 감싸고 파일 내용은 argv로만 넘기도록 — 악의적 파일명·내용의 명령 둔갑(명령 인젝션) 방지.
- 설정·문서만 변경. 검사기·진단 카탈로그·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.10 — 2026-06-14 (수동 설치 업데이트 stale 파일 안내 — 외부 평가 7차 재평가)
- **[Docs] 스킬 README 수동 설치(`<details>`)에 '업데이트 시 기존 폴더 먼저 삭제' 안내 추가**: 수동 복사(`Copy-Item .\*`/`cp -r .`)는 병합이라 옛 파일이 삭제/개명돼도 `~/.claude/skills/project-doctor`에 stale로 남는다. 루트 README의 `<details>`엔 이미 있던 경고를, P0(v2.7.9)로 동작하게 고친 스킬 README의 수동 fallback에도 정합. 자동 스크립트(install.ps1/sh)는 clean install이라 무관 — 수동 fallback만의 잔여 리스크 해소(7차 평가 재평가 잔여 지적).
- 문서만 변경. 검사기·진단 카탈로그·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.9 — 2026-06-14 (HTML url() 하드닝·버전 게이트 강화·설치 명령 교정 — 외부 평가 7차)
- **[Security] HTML 검사기 CSS `url()` 전면금지** (verify_html_report.py): 스킴 한정 정규식(`https`/`ftp(s)`/`//`)이 `url(file:·data:·blob:·behavior:)`를 놓치던 갭(7차 평가 P1a, 실측 미검출)을, §2 골격이 `url()`을 0건 쓴다는 사실에 기대 `url(` 자체를 위반으로 보는 fail-closed로 교체(`CSS_URL_RE`). 인라인 `style=`·`<style>` 양 경로 동시 적용. 골든 false-fail 0·회귀 테스트 +4(file/data/blob/inline). HTML 출력 검사기 하드닝일 뿐 진단 카탈로그·채점 무변경.
- **[CI] 버전 줄 통째 삭제도 실패** (check_version.py): main()이 마커 부재(MISSING)를 '건너뜀'으로만 처리해, 핵심문서(루트 README·스킬 README·CHANGELOG)에서 `현재 버전:`/`## v` 줄을 통째로 지우면 CI가 통과하던 false-pass(7차 평가 P2a, 실측 재현)를 차단 — 불일치·BROKEN과 마찬가지로 MISSING도 실패로 집계. find_version은 불변(단위 6테스트 유지), main() 회귀 테스트 +2.
- **[CI] `timeout-minutes: 10`** (ci.yml): 무한 루프 시 러너 6시간 점유 방어(7차 평가 P2b 중 채택분). 액션 SHA pinning은 기각(`permissions: contents: read`뿐+Dependabot 부재 → stale SHA·수동 마찰), pytest 버전 고정은 보류(BACKLOG BL-32).
- **[Docs] 스킬 README 수동설치 명령 교정** (skills/project-doctor/README.md): "이 스킬 폴더만 받았을 때" 안내(BL-30)가 정작 `skills/project-doctor` 상대경로를 복사해, 스킬 폴더만 받은 사용자에겐 그 경로가 없어 실패하던 자기모순(7차 평가 P0)을 현재 폴더(`.`) 복사로 수정. PowerShell은 `dest`가 이미 있을 때 한 단계 중첩 복사되는 `Copy-Item . dest` 함정을 피해 `.\*` 와일드카드 사용(PS5.1 실측 검증).
- 검사기·CI·문서만 변경. 진단 카탈로그(ID)·채점기·픽스처·정답지 무변경 → 탐지율 불변. (채택 4·기각/보류 5 상세: 개발 저장소 BACKLOG N.)

## v2.7.8
- HTML 결과지 보안 검증기 `tools/verify_html_report.py` 추가 (BL-31) — 허용목록 fail-closed로 낯선 태그·이벤트 핸들러·외부 리소스(인라인 style·meta refresh 포함)·위험 URI·charset 부재·비밀키 값 검사. report-formats.md §2/§5가 생성 후 호출. 검사기 추가일 뿐 탐지 카탈로그·채점 무변경.

## v2.7.7 — 2026-06-14 (체크포인트 미추적 파일 투명화 — 외부 평가 6차)
- **[Security] 실행 전 체크포인트 `git add -A` 투명화**(prescription-protocol): 체크포인트가 미추적·비gitignore 파일까지 사용자 모르게 로컬 커밋에 쓸어담던 것을 ① `git status`로 포함될 미추적 파일 표시 → ② 비밀키·개인정보(SEC-01/PII-01) 의심 시 경고·확인 → ③ -A 진행으로 보강. 비개발자가 흔히 gitignore 안 한 민감 파일을, *비밀키를 git 이력에서 찾아내는 도구*가 거꾸로 이력에 넣던 자기모순 해소. ★복원 완전성(왜 -A인가)은 유지 — 제거가 아니라 silent sweep만 차단.
- 절차 문서만 변경. 검사기·진단 카탈로그·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.6 — 2026-06-14 (SEC 패턴 정합·POSIX 복원·symlink 회귀 — 외부 평가 5차)
- **[Security] release-check SEC-01 탐지 패턴 보강**: 보고서 자가점검기 FORM-11(verify_report_format)은 잡지만 1차 탐지 지시문 SEC-01엔 없던 Google API 키(`AIza…`)·`sk-` API 키 2종을 SEC-01 탐지 목록에 추가. "보고서에 새면 잡지만 프로젝트에서 처음엔 안 찾는" 1차 탐지기↔2차 누출방지기 비대칭 해소. (외부 평가는 Slack·개인키 누락도 지적했으나 그 둘은 이미 SEC-01에 존재 — 실제 갭은 2종.)
- **[Fix] git 없는 환경 백업 복원 OS 분기**(prescription-protocol): 되돌리기 복원 명령이 PowerShell `Copy-Item` 단일이라 macOS/Linux에서 깨진 명령이 인쇄될 위험 → Windows/POSIX(`cp -R`) 분기 병기, 실행 OS에 맞는 한 줄만 인쇄하도록 명시.
- **[CI] check_write_boundary symlink 회귀 테스트 +2**: 루트 밖 가리키는 심링크가 resolve() 후 '밖' 판정(exit 1)·루트 안 심링크는 통과(exit 0)를 회귀 고정(심링크 생성 권한 없으면 skip). TOCTOU(검사 후 교체)는 도구 범위 밖(문서화된 한계)이라 검사 시점 분류만 잠근다.
- 검사기·테스트·절차 문서만 변경. 진단 카탈로그(ID)·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.5 — 2026-06-14 (검사기 하드닝 — 전체 코드 리뷰 Optional 반영)
- **[Fix] 표지 식별·주버전 판정을 표지 첫 코드블록으로 한정** (verify_report_format): `check_cover`(FORM-02/03/04)·`detect_major_version`이 전체 줄을 봐서, 표지에 실제 `스킬 버전:` 줄이 없어도 본문 펜스 안 예시(`스킬 버전: v1.0.0`)가 표지를 대신해 FORM-03을 충족하고 major를 v1로 오판→숙제(FORM-10)를 건너뛰던 우회를 봉합. 새 `cover_block()`으로 첫 코드블록만 판정에 쓴다(제목 헤딩은 펜스 밖이라 `detect_mode`가 따로 봄).
- **[Fix] check_version.find_version 깨진-최신 헤딩 누락**: 마커(`## v`)가 여러 줄인 CHANGELOG에서 최신(첫) 헤딩이 깨졌을 때 그 아래 과거 정상 헤딩으로 새던 것을, 첫 마커 줄에서 판정 확정(없으면 BROKEN)으로 수정. docstring '첫 줄을 보고'와 동작 일치.
- **[Robustness] 위치 셀 길이 상한**(check_location_sampling): `FILENAME_RE`가 비정상적으로 긴 셀에서 catastrophic backtracking(O(n²)·10만 자 122초)을 내던 것을 `MAX_LOC_LEN`(2000) 상한으로 차단. 현실 셀(<100자) 정상 추출엔 무영향.
- **[CI] 회귀 테스트 +14**(총 84): `test_check_version`(4분기)·`test_run_checks`(게이트 합성)·`test_check_scoring_regression` 신설 + 표지 펜스·위치 상한 회귀. 다른 5개 검사기에만 있던 `test_*.py` 규율을 check_version/scoring/run_checks까지 확장.
- 검사기·테스트·문서(버전)만 변경. 진단 카탈로그·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.4 — 2026-06-14 (FORM-12 빈 기계블록 사각지대 보강 — Codex 리뷰)
- **[Fix] FORM-12 빈 기계블록(`발견ID: (없음)`) 사각지대**: 본문 `###` 발견 항목이 0개이고 `## 부록`에만 카탈로그 ID가 있을 때, `check_four_fields`의 `if not ids: return`이 FORM-12 교차검사에 도달하기 전에 반환해 "부록 전용 ID ↔ `발견ID: (없음)`" 모순을 통과시키던 빈틈을 봉합. 이 분기에서도 `collect_body_catalog_ids`로 부록 ID를 교차검사한다 — report-template 9절("발견ID 블록엔 본문·부록 구분 없이 모든 발견 ID를 빠짐없이") 위반을 검출. 사각지대 조건은 (발견ID:(없음)) AND (### 0개) AND (부록 ID ≥1)로 좁다. 회귀 테스트 +1(총 70).
- 검사기·테스트·문서(버전)만 변경. 진단 카탈로그·채점기·픽스처·정답지 무변경 → 탐지율 불변.

## v2.7.3 — 2026-06-14 (발견ID 양방향 무결성 — 외부 코드 리뷰 P0)
- **[P1] FORM-12 — 사람용 본문/부록 ↔ 기계 발견ID 블록 양방향 일치 검사**: 발견 항목 제목(`### … [ID]`)·부록(7절) 항목의 ID 집합과 보고서 끝 `발견ID:` 줄의 ID 집합이 정확히 일치하는지 verify가 강제 — report-template 9절 "발견ID 블록에는 본문·부록 구분 없이 모든 발견 ID를 빠짐없이" 계약의 자동화. 한쪽에만 있는 ID(발견ID 줄엔 DUP-02 있는데 본문엔 [DUP-01]만, 또는 그 반대)를 검출해 채점기 탐지율 오집계·근거 없는 ID를 차단. 기존엔 발견ID 블록이 비었거나(`(없음)`) 본문 발견이 0개인 '존재 수준' 단방향만 잡았음(set 일치 검사 부재)
- 재검진 비교표(4절)·처치 경과 줄에 인용된 과거 ID는 '이번 검진의 발견'이 아니므로 수집 범위 밖(거짓양성 방지). 제목 ID 누락(FORM-09)이 있으면 본문 집합이 불완전하므로 교차검사 보류 — 제목 ID부터 채워야 의미를 가지며 같은 뿌리를 두 번 보고하지 않음
- **[CI] 회귀 테스트 +3**: 발견ID 줄에만 있는 ID·본문에만 있는 ID 검출, 부록 ID 정합(거짓양성 가드)
- 검사기·테스트·문서(버전)만 변경. 진단 카탈로그·채점기·픽스처·정답지 무변경

## v2.7.2 — 2026-06-13 (검사기 계약 통일·보안 문서 정합 — 외부 코드 리뷰)
- **[P1] 검사기 간 발견ID 계약 통일**: verify_report_format이 코드펜스 안 `발견ID:`를 실제 블록으로 오인(통과)하던 것을 raw-line만 인정하도록 수정 — compare_report(펜스 무시)와 일치. 같은 보고서가 "형식 통과·채점 0%"로 갈리던 모순 제거. test_fenced_id_block_passes를 새 계약(FORM-05) 회귀로 전환
- **[P1] 보안 문서 충돌 해소**: release-checklist SEC-02의 "이력 스캔에서 EXPECTED.md 제외"를 SKILL v2.7.1 규칙9와 정합 — 보안 스캔은 표식 있는 EXPECTED.md도 검사(채점만 제외). v2.7.1 비밀키 우회 차단이 SEC-02에서 되살아나던 구멍 봉합
- **[P2] 발견 제목 ID 필수 검사**: verify가 발견 항목 제목의 카탈로그 ID(`[DUP-01]` 형식) 누락을 FORM-09로 검출 (재검진·승인 명령 추적 토대)
- **[P2] 숙제 줄 펜스 우회 차단**: check_homework를 outside_fence 기반으로 — 펜스 안 예시 `숙제:`가 실제 줄을 대신 못 함. 중복 줄도 검출
- **[P3] 저장소 밖 링크 검출**: check_links가 `../`로 저장소 밖을 가리키는 링크를 (러너에 우연히 존재해도) 실패로 처리
- **[Low] compare 참고 출력 펜스 정합**: parse_revisit_lines(비교:/숙제:)도 펜스 무시로 통일
- **[CI] golden 보고서 E2E 추가**: 실보고서 샘플(`tests/golden/checkup-report.md`)이 verify+compare 동시 통과하는지 매 CI 검사 — 계약 불일치 재발 방지. 회귀 테스트 +3(제목ID·숙제펜스·저장소밖링크)
- 검사기·CI·보안 문서만 변경. 진단 카탈로그·픽스처·정답지 무변경

## v2.7.1 — 2026-06-13 (보안 하드닝 — 다중에이전트 감사 3건)
- **[High] HTML/PDF 결과지 이스케이프 의무**(report-formats.md 2장): 분석 대상 프로젝트에서 온 모든 값(파일명·소견·코드 인용·프로젝트명)을 HTML에 넣기 전 5개 문자(`& < > " '`) 엔티티 치환 절대 규칙 추가. 악성 파일명(`</style><img onerror=…>.py` 등)이 결과지에 raw로 박혀 더블클릭·PDF 변환(headless 브라우저 `file://` 로드) 시 실행되던 저장형 XSS 경로 차단. `<script> 금지`는 작성 제약이라 데이터 passthrough를 못 막음 → 이스케이프를 별도 명문화
- **[High] 정답지 제외의 비밀키 우회**(SKILL.md 절대 규칙 9): 표식 단 `EXPECTED.md` 제외를 "진단 채점 한정"으로 좁히고, SEC-01·SEC-02·PII-01·HARD-01 스캔은 제외 파일도 항상 검사하도록 카브아웃 추가(표식은 위조 가능한 평문 → 비밀키 은닉처가 되어 거짓 "통과"를 내던 경로 차단, TMP-01 보관함 예외와 동일 원칙)
- **[Medium] HTML/PDF 비밀키 자가 점검**(report-formats.md 5장): FORM-11(비밀키 값 노출 자동 검사)이 .md만 보므로, HTML/PDF 저장 전 키 값 부재를 md와 동일 기준으로 직접 대조하도록 의무화
- **[위생] CI 최소권한**(ci.yml): `permissions: contents: read` 추가
- **[문서] DEMO·EVALS 최신화**(외부 리뷰 1순위, BL-27): dev DEMO.md를 v2.7.1 결과지 양식으로 재작성(발견ID 맨 마지막 줄, static_data.js 1,011줄 실측 보정) — 형식 검증기 위반 0 + 채점기 14/14·오탐 0 통과. EVALS checkup 표에 v2.7.1 행 + v2.5~v2.7.1 탐지 무영향 분석 추가
- 절차 문서·CI·문서만 변경. 진단 카탈로그·채점기·픽스처·정답지 무변경

## v2.7.0 — 2026-06-12 (보관함·버전 사본 탐지 + 공개 전 검진 보강 — BL-03·04·06)
- TMP-01: 버전 사본 더미(같은 이름+버전 번호 3개 이상) 묶음 탐지(BL-04), 정리용 폴더(archive/old/backup) 흡수 규칙(BL-03, 치료↔재검진 충돌 해소, 비밀키·개인정보 예외)
- release-check: 도구 설정 파일 개인 경로·광역 권한·git 추적 점검(BL-06)
- messy 회귀 100% 유지, 픽스처·정답지·ID 인벤토리 무변경. 공개판 v1.7.0과 정의 동일

## v2.6.0 — 2026-06-12 (HARD-02 검진 일관성 — BL-01)
- HARD-02(반복 매직 넘버) 탐지 기준 명확화: 대상(요율·한도·임계 등 비즈니스/설정 값)·제외(0·1·-1, 배열 인덱스, 화면 좌표·여백 등 구조적 수치) 구분. 기존 DUP-01 흡수 규칙(v2.0)에 대상/제외를 보강
- 효과: 신선 검진자 3명 재측정 전원 동일 판단·탐지율 100% (이전엔 검진자마다 매직 넘버 꼬리 흔들림). 공개판 v1.6.0과 정의 동일
- 진단 카탈로그 정의 변경, 픽스처·정답지 무변경

## v2.5.3 — 2026-06-12 (비긴급 백로그 — 위생 정리)
- check_links: 이미지 임베드(`![]()`)·코드펜스 안 예시 링크를 검사에서 제외(오탐 방지) + 단위 테스트 6건 신설
- 출력 이모지(`✓`) 제거 — 사용자 환경 규칙(이모티콘 금지) 정합
- docstring BACKLOG 번호에 역할 명시
- 코드 동작·채점·보고서 계약 무변경. (DEMO 발견ID 위치 수정은 공개판 v1.5.3 한정 — dev DEMO는 구식 양식이라 BL-27로 별도)

## v2.5.2 — 2026-06-12 (전체 코드 리뷰 결함 수정 — 채점기·검증기 파싱 견고성)
- verify: 코드펜스 등급 우회(Critical)·비밀키 거짓음성(GitHub 가변길이·Google 키)·4요소 substring 오판·cp949 트레이스백·버전 오판 수정
- compare: 펜스 예시 발견ID 제외, 전각파이프·백틱·공백헤더·2표·GUID함정·절진입 견고화, neutral ISO 오추출 수정
- 회귀 테스트 +17(총 50). 진단·보고·채점 계약 무변경 (공개판 v1.5.2와 동일 수정)

## v2.5.1 — 2026-06-12 (코드 리뷰 결함 수정 — 검증자 실측 재현 8건)
- **[Critical] check_write_boundary**: 드라이브 상대경로(`C:foo`)·UNC 경계 우회 수정(거부) + TOCTOU 한계 문서화·해석경로 출력 + 빈/공백 경로 거부
- compare_report: cp949 보고서 트레이스백 → `errors="replace"`(형제 스크립트와 일관화)
- check_scoring_regression: 합성 보고서 tempfile화(레이스·잔존·동명파괴 제거), 채점기와 동일 ID추출(`parse_expected_ids`) 재사용, "N개 중 M개 채점" 분모 출력
- check_version: 버전 깨짐을 표기없음과 구분해 실패 집계 + 단어경계 정규식
- check_links: title 문법·쿼리스트링 분리로 오탐 제거
- 단위 테스트 +3, 양축 CI. 진단·보고·채점 계약 무변경 (공개판 v1.5.1과 동일 수정)

## v2.5.0 — 2026-06-12 ("쓰기 경계 자동 검사")
- 치료 실행 안전 강화: `tools/check_write_boundary.py` 신설 — 변경 예정 경로가 프로젝트 폴더 밖(절대 경로·`../` 탈출)이면 비0 종료. 스킬과 함께 설치돼 치료 직전 호출 가능. 읽기 전용
- prescription-protocol 0장 4번(쓰기 경계 확인)에 도구 호출 안내 추가 — 없으면 수동 폴백
- 단위 테스트 7건 + CI(GitHub Actions: pytest·채점 회귀·링크 검사) 신설, tests/run_checks.py·check_links.py 추가. 외부 코드리뷰(7.3/10) 반영 B묶음 일부(BL-15·16·18)
- 진단·보고·채점 계약 무변경

## v2.4.0 — 2026-06-12 ("클리니컬 결과지 + PDF 직접 출력")
- HTML 결과지를 **"클리니컬" 디자인**으로 확정 (사용자 시안 3종 비교 선택): 종합병원 검진센터 결과지 스타일 — 타이포그래피 위계·가는 괘선 중심, 이모지·아이콘 장식 금지, 색은 판정·심각도에만 최소 사용. CSS 골격·클래스를 디자인 계약으로 고정
- **PDF 직접 출력 신설**: 이 컴퓨터에 이미 있는 브라우저(Edge/Chrome headless)를 감지해 HTML→PDF 자동 변환 — 별도 설치 0, 감지 실패 시 "브라우저 인쇄→PDF 저장" 안내로 폴백. 형식 질문에 ③ PDF 추가
- 형식 변형 묶음 확장: 같은 보고서의 .md+.html+.pdf = 보고서 1건 (절대 규칙 7)
- 보고서 내용·채점 계약 무변경

## v2.3.1 — 2026-06-12 ("HTML 결과지 디자인 개편")
- 사용자 피드백("전문적이면서 비개발자도 직관적으로") 반영 — HTML 인쇄용을 "병원 결과지" 디자인으로 재설계: 머리띠(환자명)+메타 줄, 종합 등급 원형 배지(등급별 색), 심각/권장/참고 건수 칩, 부위별 판정 알약(pill), 오늘의 처방 강조 상자, 발견 항목은 심각도별 색 테두리 카드(4요소 표), 부록·기계 판독 꼬리 유지
- CSS 골격·클래스를 report-formats.md에 디자인 계약으로 고정 (회차 간 모양 흔들림 차단, 임의 변경 금지)
- 자기완결 의무(외부 리소스·script 금지)·인쇄 규칙(@media print, 카드 페이지 분리 방지) 동일 유지

## v2.3.0 — 2026-06-12 ("보고서 형식 선택")
- 보고서 저장 시 형식 선택 신설 (`references/report-formats.md`): ① 마크다운(기본) ② **HTML 인쇄용** — 더블클릭→브라우저→인쇄(Ctrl+P)→"PDF로 저장"으로 PDF화, 워드에서도 열림 ③ 워드(.docx)는 pandoc이 설치된 컴퓨터에서만 변환(설치 유도 금지, 없으면 HTML 폴백 안내)
- HTML 자기완결 의무: 인라인 CSS·외부 리소스(CDN/웹폰트) 금지·`<script>` 금지·UTF-8 — 오프라인/사내망에서도 안전하게 열림. 기계 판독 줄(비교:·숙제:·발견ID:)은 문서 끝에 작게 포함(생략 금지), 채점 정본은 여전히 md/채팅
- 절대 규칙 7 보정: 같은 보고서의 형식 변형(.md+.html)은 보고서 1건으로 간주
- 보고서 내용·계약(결과지 3블록·ID+4요소·기계 판독 블록) 무변경 — 형식은 겉포장만

## v2.2.0 — 2026-06-12 ("처방 미리보기 + 형식 검증기")
- 처방 실행 게이트 확장: 변경 예정 "파일 목록"을 "파일 목록 + 전/후 변경 내용 미리보기"로 — 승인 전에 무엇이 어떻게 바뀌는지 일상 언어 설명과 함께 확인, 실행 중 미리보기 밖 변경이 필요해지면 중단 후 재제시
- 보고서 형식 검증기 신설 (`tests/verify_report_format.py` + pytest 11건): 표지·스킬 버전 줄·등급↔표현 고정표 정합·4요소 개수 일치·기계 판독 블록 위치·비밀키 값 노출 휴리스틱을 기계 검사 — 측정 루프에 "무엇을 찾았나"(채점기)와 별개로 "약속한 모양인가"(형식) 게이트 추가. 숙제 줄 검사는 v2 이상 보고서에만 적용 (v1.x 공개판 보고서 오탐 방지, 실보고서 8장 전수 검증)
- SKILL.md 보고서 공통 의무에 "내보내기 전 자가 점검" 추가 (빠진 요소 채우기 + 비밀키 값/위치 확인)
- 스킬 동작 변경은 처방 게이트 1곳 — 진단·보고·채점기 계약(발견ID·비교:·숙제:) 무변경
- 보고서 머리를 '건강검진 결과지' 3블록으로 재설계: 표지(환자·검진일) → 종합 판정(등급 크게 + 읽는 법 범례 + 의사 소견) → 부위별 소견(신호등 판정) + 오늘의 처방(당장 1건·재검 권고)
- 등급↔표현 고정표 신설 (A🟢건강 / B🟢양호 / C🟡주의 / D🔴치료 필요) — 회차 간 판정어 흔들림 차단
- 소견-판정 톤 정합 규칙 ("B인데 '하나도 없습니다'"식 충돌 금지), 종합 등급 산정 설명("평균이 아니라 합산")을 보고서 머리로 이동
- v2.0의 8절 처방 줄을 3절 "오늘의 처방"으로 이동 (기계 판독 줄 `비교:`·`숙제:`는 8절 유지 — 채점기 계약 변경 없음)
- 교차 참조 갱신 6개 문서 (SKILL·catalog·intro·pivot·record·release)

## v2.0.1 — 2026-06-10 (측정 보강 패치)
- 채점기 오탐 자동 게이트: 정답지("심은 문제"·"추가 보고 허용")에 없는 ID 보고 시 자동 미달 — 기존엔 참고 출력만 했음 (pytest 11건으로 확장)
- 채점기 BOM 허용: PowerShell 5.1이 만드는 BOM 붙은 UTF-8 보고서도 채점 가능 (utf-8-sig)
- 정답지 정비: leaky-project에 "추가 보고 허용(SEC-02 이력 사본)" 절 추가, 통과 기준에 오탐 게이트 명문화
- report-template: 등급 규칙 안내의 죽은 문구("S2에서 확정") 제거 — 카탈로그 "검진 결과표 등급 규칙" 절 직접 지시
- EVALS: 한계 4번(ID 일치 채점·게이트의 ID 수준 한계) 추가, v2.0 기능(단골 차트) 측정 기록 추가
- 스킬 동작(진단·보고·치료 절차) 변경 없음 — 문서·측정 도구만

## v2.0.0 — 2026-06-10 ("단골 주치의")
- 단골 차트: 2회째 방문부터 머리말에 등급 추이 1줄 ("5월 D → 오늘 C")
- 수술 경과: 치료했던 항목의 재발 여부를 비교표 위에 선표시
- 처방전: 보고서가 "이번 주 처방 1건 + 다음 검진 권장일 질문"으로 마무리 (8절 확장, 기계 판독 줄 `비교:`·`숙제:` 추가)
- 검진 주기 확인: 등급→주기 고정표(A 90/B 60/C 30/D 14일), 어느 모드로든 진입 시 환영형 안내 (checkup 제외)
- 후기 고리(1회 한정), 추이 화법 3종·숙제 복귀 문안(책망 금지), 기록부 거부 강등 규칙, 구버전 기록 조용한 생략
- 기록부 양식 확장: 숙제·다음 검진 권장일·후기 요청 이력 필드
- 재방문 시드 픽스처(revisit-seeds 2종 + 정답지), 채점기 재방문 줄 파싱 + pytest 7건

## v1.1.0 — 2026-06-10 (v1.1 컷)
- 공개 전 검진(release-check): 키 형식별 패턴(SEC-01) + git 과거 기록 점검(SEC-02) + 개인정보(PII-01) + 라이선스·기록부 잔존(REL) + 통과/보류 판정(시점 기재 + 한계 고지 3종) + 폴더 검사 제외 선언
- 초진 차트(intro): 프로젝트 지도(핵심 파일 5개)·실행법·위험 구역·미완성 구간 + 인수인계 소견서 옵션(양방향)
- 방향전환(pivot): 갈림길 비교(2차 소견) + 유지/수정/폐기 매트릭스 + 마일스톤("멈춰도 동작") + 폐기 2단계
- 접수 문진 완전판(4모드 라우팅), 픽스처 3종(leaky·handover·pivot) + 정답지, 전체 회귀 + 개밥먹기
- 라이선스 MIT 확정, 스킬 전용 배포 저장소 분리 (Q3·Q6 해소)

## v1.0.0 — 2026-06-10 (v1.0 컷)
- 정밀 검진(--deep) 추가: git 이력 핫스팟(HIST-01), 의존성 대조(DEP-01/02), 검사 장비 통역(LINT-01, 코드 실행 승인 게이트), AI 작업물 문진(AIGEN, 보수적 판정)
- 진단 카탈로그 25개+ 본문 완성 (기본 검진 22항목 + 정밀 전용), 규모 가드(파일 100개 초과 시 축소 제안)
- 진료기록부: `.project-doctor/records/` 생성 승인 게이트 + 자체 .gitignore + 재검진 비교(해결/그대로/신규/악화)
- README 완성판(필수 고지 3종) · LICENSE · CHANGELOG · 채점 스크립트 pytest
- 정식 측정: 신선한 세션 3회 검진, 최저치 기준 80% 이상 통과

## v0.3.0 — 2026-06-10
- 실행 처방전 프로토콜: 실행 게이트(파일 목록 제시·사전 질문) → 체크포인트 → 변경 → 검증 사다리 → 전/후 비교 → 되돌리기 한 줄 항상 인쇄
- git 없는 환경 대체 절차(백업/거부 시 실행 차단), 쓰기 경계(프로젝트 루트 한정)

## v0.2.0 — 2026-06-10
- 건강검진(진단·보고) 활성화: ID 불변 계약 + 진단 카탈로그 코어 10항목
- 테스트 픽스처(messy-project) + 정답지(EXPECTED.md) + 기계 채점 스크립트 — 드라이런 탐지율 100%
- 보고서 공통 골격 최종 동결 (검진 결과표 + ID+4요소 + 기계 판독 블록)

## v0.1.0 — 2026-06-10
- 뼈대: 접수 문진, 인자 해석 표, 절대 규칙 12개, 모드 라우팅, README 스텁
