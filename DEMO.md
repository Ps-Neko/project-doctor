# 3분 데모 — 실제 진단 보고서

> 아래는 꾸민 예시가 아니라, 이 저장소의 테스트 프로젝트(`tests/fixtures/messy-project` — 일부러 문제 14건을 심어둔 가짜 쇼핑몰 도구)를 실제로 검진한 **측정 산출물 원문**입니다. 같은 명령을 직접 실행하면 같은 구조의 보고서를 받습니다.

## 입력

```
/project-doctor checkup "tests/fixtures/messy-project"
```

## 결과 요약

- 발견 16건: **심각 3 · 권장 7 · 참고 6** · 종합 등급 **D**
- 심은 문제 14건 전부 탐지 (탐지율 100% — [측정 기록](./EVALS.md)) + 정답지 밖 정당한 발견 2건 (본문 사실 확인 거침)

---

## 실제 보고서

# 🏥 프로젝트 건강검진 결과지 — messy-project

```
환자: messy-project · 검진일: 2026-06-11 · 분석 파일: 11개 (제외 목록 적용 후 — 자체 테스트 시 정답지도 제외)
스킬 버전: v1.2.0 · 모드: 건강검진
검진 대상: tests/fixtures/messy-project
```

## 종합 판정

# 🔴 D — 치료가 필요해요

> 등급 읽는 법: 🟢 A 건강 · 🟢 B 양호 · 🟡 C 주의 · 🔴 D 치료 필요
> ※ 종합은 부위 평균이 아니라 발견 건수·심각도의 **합산 점수**입니다 — 부위 등급보다 낮을 수 있어요.

**의사 소견**: 작은 프로젝트인데 손볼 곳이 골고루 쌓여 있습니다. 특히 돈 계산 함수가 3개 파일에 복사된 것(심각 1)이 가장 위험하니 거기부터 잡는 걸 권합니다.

## 부위별 소견

| 부위 | 판정 | 소견 |
|------|------|------|
| 구조 | 🔴 치료 필요 (D) | 돈 계산 함수가 3개 파일에 복사되어 있고, 1,000줄짜리 파일에 데이터와 로직이 섞여 있어요 |
| 정리 | 🟡 주의 (C) | 백업 파일·실험 파일·안 쓰는 함수·주석 처리된 옛 코드가 그대로 남아 있어요 |
| 문서 | 🟡 주의 (C) | 설명서(README)도, 어떤 부품(외부 패키지)을 쓰는지 적은 목록도 없어요 |
| 테스트 | 🟢 양호 (B) | 자동 확인 장치(테스트)는 없지만, 규모가 작아 당장 위험하진 않아요 |
| 안전 | 🟡 주의 (C) | 특정 컴퓨터 전용 폴더 주소와 내부 서버 IP가 코드에 박혀 있어요 |

## 오늘의 처방

진단 결과: 🔴 심각 3건 · 🟡 권장 7건 · ⚪ 참고 6건
👉 **당장 1건만 하신다면**: 심각 1(돈 계산 함수 3곳 복사) — 지금 "심각 1 실행해줘"라고 하시면 제가 고칩니다.
📅 **재검 권고**: 2026-06-25 (등급 기준 — A 90일 / B 60일 / C 30일 / D 14일 뒤)

## 발견 항목

> 발견이 10건을 초과해(16건) 본문에는 심각 전부 + 권장 상위 5건만 싣고, 나머지 8건은 끝의 부록에 한 줄씩 정리했습니다.

### 🔴 심각 1 [DUP-01] — 돈 계산 함수(calc_order_total)가 3개 파일에 복사되어 있음
- **무슨 뜻인가요?** 할인·배송비를 계산하는 똑같은 코드가 3곳에 있습니다. 할인 정책이 바뀌면 3곳을 전부 고쳐야 하고, 한 곳이라도 빠뜨리면 금액이 다르게 계산되는 버그가 됩니다. 게다가 지금 실제로 쓰이는 건 main.py의 사본 하나뿐이고, 나머지 2곳은 불러가는 곳도 없습니다.
- **어디?** `main.py:15`, `helpers.py:26`, `utils.py:17`
- **고치면?** 한 곳에만 남기고 나머지는 그걸 불러 쓰게 됩니다. 예상 작업: 파일 3개 수정.
- **승인 명령:** "심각 1 실행해줘"

### 🔴 심각 2 [HARD-01] — 특정 컴퓨터에만 있는 폴더 주소가 코드에 박혀 있음
- **무슨 뜻인가요?** 주문 데이터 파일 위치가 `C:\Users\kim\...`처럼 특정 컴퓨터 전용 주소로 적혀 있습니다. 다른 컴퓨터에서는 그 주소가 없어서 실행하자마자 오류가 납니다. (코드에 이미 "config.json으로 옮기기" TODO 메모가 있습니다.)
- **어디?** `main.py:12` (`DATA_FILE = r"C:\Users\kim\Desktop\orders\data.csv"`)
- **고치면?** 데이터 경로를 설정 파일(config.json)로 옮겨 어느 컴퓨터에서나 돌게 됩니다. 예상 작업: 파일 2개 수정.
- **승인 명령:** "심각 2 실행해줘"

### 🔴 심각 3 [DOC-01] — 프로젝트 설명서(README)가 없음
- **무슨 뜻인가요?** 이 프로젝트가 뭐 하는 것인지, 어떻게 켜는지 알 방법이 코드뿐입니다. 다른 사람(그리고 6개월 뒤의 나)이 처음부터 코드를 뜯어봐야 합니다. (`보고서 초안.md`는 업무 메모라 설명서 역할을 못 합니다.)
- **어디?** 프로젝트 최상위 폴더 (README* 0건)
- **고치면?** 한 페이지짜리 README.md가 생겨 소개·실행 방법을 바로 알 수 있게 됩니다. 예상 작업: 파일 1개 신설.
- **승인 명령:** "심각 3 실행해줘"

### 🟡 권장 1 [DEAD-01] — 만들어 두고 아무도 부르지 않는 함수
- **무슨 뜻인가요?** 구버전 정책이라고 적힌 `legacy_discount` 함수를 프로젝트 어디에서도 부르지 않습니다. 읽는 사람의 시간을 뺏고 "이거 지워도 되나?"라는 고민을 남깁니다.
- **어디?** `utils.py:33` (`legacy_discount` — 정의 외 참조 0건)
- **고치면?** 안 쓰는 코드가 사라져 파일이 읽기 쉬워집니다. 예상 작업: 파일 1개 수정.
- **승인 명령:** "권장 1 실행해줘"

### 🟡 권장 2 [BIG-01] — 1,000줄이 넘는 거대 파일
- **무슨 뜻인가요?** `static_data.js`가 1,019줄입니다. 책으로 치면 목차 없는 1,000쪽짜리 챕터라 원하는 부분을 찾기도 고치기도 어렵습니다. 이름은 "정적 데이터"인데 상품 목록(데이터)과 조회·세금 계산(로직)이 한 파일에 섞여 있어, 순수 데이터 파일로 봐줄 수도 없습니다.
- **어디?** `static_data.js` (1,019줄 — 임계값 800줄 초과)
- **고치면?** 데이터 파일과 조회 함수 파일로 나뉘어 가격 수정·기능 수정이 서로 안전해집니다. 예상 작업: 파일 1개를 2개로 분리, 참조 1곳(index.html) 수정.
- **승인 명령:** "권장 2 실행해줘"

### 🟡 권장 3 [HARD-03] — 내부 서버 IP가 코드에 직접 박혀 있음
- **무슨 뜻인가요?** 주문 API 주소가 `http://192.168.0.10/...`처럼 코드에 직접 적혀 있습니다. 서버가 바뀌면 코드를 고쳐야 하고, 내부 주소가 외부에 노출될 수도 있습니다. (코드에 이미 "설정 파일로 옮기기" FIXME 메모가 있습니다.)
- **어디?** `helpers.py:3` (`ORDERS_API_URL`)
- **고치면?** 주소가 설정 파일(config.json)로 옮겨져 서버가 바뀌어도 설정만 고치면 됩니다. 예상 작업: 파일 2개 수정.
- **승인 명령:** "권장 3 실행해줘"

### 🟡 권장 4 [DOC-03] — 어떤 부품(외부 패키지)을 쓰는지 적은 목록이 없음
- **무슨 뜻인가요?** 파이썬 코드가 외부 패키지 `requests`를 쓰는데, 무엇을 설치해야 하는지 적은 목록(requirements.txt)이 없습니다. 새 컴퓨터에서 돌리려면 시행착오로 알아내야 합니다.
- **어디?** `utils.py:6` (`import requests`) — `requirements.txt`·`pyproject.toml` 0건
- **고치면?** 목록 파일이 생겨 명령 한 줄로 설치 환경을 똑같이 만들 수 있습니다. 예상 작업: 파일 1개 신설.
- **승인 명령:** "권장 4 실행해줘"

### 🟡 권장 5 [TEST-01] — 자동 확인 장치(테스트)가 하나도 없음
- **무슨 뜻인가요?** 고친 뒤에 "아직 잘 도는지" 자동으로 확인해 주는 안전장치가 없습니다. 지금은 고칠 때마다 사람이 일일이 돌려봐야 합니다. (`test123.py`는 이름만 test일 뿐 실험 메모라 테스트가 아닙니다.)
- **어디?** 프로젝트 전체 (tests/ 폴더·test_*.py·*.test.js 등 0건)
- **고치면?** 돈 계산 같은 핵심 함수부터 자동 검사가 생겨, 고칠 때마다 몇 초 만에 이상 유무를 알 수 있습니다. 예상 작업: 테스트 파일 1~2개 신설.
- **승인 명령:** "권장 5 실행해줘"

## 부록: 나머지 발견 항목

| ID | 심각도 | 한 줄 요약 |
|----|--------|-----------|
| [TMP-01] | 🟡 | 예전 버전 백업 파일 `old_main.py.bak`이 남아 있음 — 진짜 코드와 헷갈릴 위험 |
| [STALE-02] | 🟡 | 실험하다 두고 간 파일 `test123.py` ("나중에 지울 것" 메모, 어디서도 참조 안 됨) |
| [DEAD-02] | ⚪ | 주석 처리된 옛 코드 덩어리 약 15줄 (`helpers.py:60-75` — 옛 계산·쿠폰 함수) |
| [BIG-02] | ⚪ | 함수 하나가 68줄 (`main.py:42-109` `process_orders` — 변환·검증·집계·출력이 한 함수에) |
| [BIG-03] | ⚪ | 코드가 4겹 넘게 안으로 파고듦 (`helpers.py:42-57` `flag_risky_orders` — 최대 6겹) |
| [HARD-02] | ⚪ | 의미 있는 값(50000·30000·3000 등)이 이름 없이 3회 이상 반복 — config.json에 같은 값이 있는데 코드는 안 씀 |
| [STALE-01] | ⚪ | "나중에 고치자" 메모(TODO·FIXME) 6건 누적 (`main.py` 3건, `helpers.py` 3건) |
| [STRUCT-03] | ⚪ | 라벨과 내용물이 다른 서랍 — `static_data.js`라는 이름인데 조회·세금 계산 로직이 들어 있음 |

## 다음 모드 연계 추천

이 프로젝트를 회사 공유나 GitHub 공개 전이라면 `release-check`(공개 전 검진)를 받아보세요 — 비밀키·개인정보까지 점검합니다.

발견ID: DUP-01, DEAD-01, DEAD-02, BIG-01, BIG-02, BIG-03, HARD-01, HARD-02, HARD-03, DOC-01, DOC-03, TEST-01, TMP-01, STALE-01, STALE-02, STRUCT-03

---

## 이 다음은?

보고서에서 항목을 골라 `"심각 2 실행해줘"`라고 하면 — 변경 파일 목록 제시 → 체크포인트 → 수정 → 검증 → **되돌리기 명령 한 줄**까지 자동으로 진행됩니다. 승인하지 않은 항목은 1byte도 건드리지 않습니다.
