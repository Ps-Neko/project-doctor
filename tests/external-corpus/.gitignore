# 외부(타인) 프로젝트 코드는 커밋하지 않는다 — 정답지·메모·이 틀(README/_TEMPLATE)만 추적.
# (BACKLOG BL-14: 외부 검증 코퍼스). 코드 일반(*.py·*.js 등)은 무시하고
# 어느 깊이의 EXPECTED.md·NOTES.md 만 화이트리스트로 되살린다.
*
!*/
!.gitignore
!README.md
!EXPECTED.md
!NOTES.md
